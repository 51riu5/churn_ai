services:
  - type: web
    name: churn-ai-dashboard
    runtime: python
    buildCommand: pip install -r requirements-deploy.txt
    startCommand: uvicorn src.api.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.7"
